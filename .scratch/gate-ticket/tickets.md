# Tickets: Gate ticket for live gateways

## Round 1 — core policy + TDX enforcement

### T1. Pure ticket crypto + claims — [x]
### T2. Pure live-gateway dual-auth decision — [x]
### T3. Mint/escalation process stores — [x]
### T4. Mint HTTP orchestration + `api/gate.ts` + Express route — [x]
### T5. Enforce on `runTdxProxyHttp` + adapters — [x]

## Round 2 — other live gateways + SPA client + review

### T6. Enforce on geocode + youbike (Vercel + vite dev) — [x]
### T7. SPA `gateTicketClient` + wire live fetches — [x]
### T8. Env docs (`GATE_SECRET`), package test script, CORS Allow-Headers — [x]
### T9. Regression: lint + focused suites; code-review fixes — [x]
